services:
    # laravel.test:
    #     build:
    #         context: '.'
    #         dockerfile: Dockerfile
    #         args:
    #             WWWGROUP: '${WWWGROUP:-1000}'
    #     extra_hosts:
    #         - 'host.docker.internal:host-gateway'
    #         - 'pupsrc-aes.test:host-gateway'
    #         - '*.pupsrc-aes.test:host-gateway'
    #     ports:
    #         - '${APP_PORT:-80}:80'
    #         - '${VITE_PORT:-5173}:${VITE_PORT:-5173}'
    #     environment:
    #         WWWUSER: '${WWWUSER:-1000}'
    #         USE_SUPERVISOR: "true"
    #         COMPOSER_UPDATE_ON_START: '${COMPOSER_UPDATE_ON_START:-false}'
    #         RUN_MIGRATIONS_ON_START: '${RUN_MIGRATIONS_ON_START:-false}'
    #         LARAVEL_SAIL: 1
    #         XDEBUG_MODE: '${SAIL_XDEBUG_MODE:-off}'
    #         XDEBUG_CONFIG: '${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}'
    #         IGNITION_LOCAL_SITES_PATH: '${PWD}'
    #     volumes:
    #         - '.:/var/www/html'
    #     entrypoint: ["/var/www/html/docker/entrypoint.sh"]
    #     networks:
    #         - sail
    #     depends_on:
    #         - pgsql
    #         - rabbitmq
    pgsql:
        image: 'postgres:17-alpine'
        ports:
            - '${FORWARD_DB_PORT:-5432}:5432'
        environment:
            PGPASSWORD: '${DB_PASSWORD:-secret}'
            POSTGRES_DB: '${DB_DATABASE}'
            POSTGRES_USER: '${DB_USERNAME}'
            POSTGRES_PASSWORD: '${DB_PASSWORD:-secret}'
        volumes:
            - 'sail-pgsql:/var/lib/postgresql/data'
            - './vendor/laravel/sail/database/pgsql/create-testing-database.sql:/docker-entrypoint-initdb.d/10-create-testing-database.sql'
        networks:
            - sail
        healthcheck:
            test:
                - CMD
                - pg_isready
                - '-q'
                - '-d'
                - '${DB_DATABASE}'
                - '-U'
                - '${DB_USERNAME}'
            retries: 3
            timeout: 5s
    pgadmin:
        image: 'dpage/pgadmin4:latest'
        ports:
            - '${FORWARD_PGADMIN_PORT:-8080}:80'
        environment:
            PGADMIN_DEFAULT_EMAIL: 'admin@example.com'
            PGADMIN_DEFAULT_PASSWORD: '${PGADMIN_PASSWORD:-admin123}'
            PGADMIN_CONFIG_SERVER_MODE: 'False'
        volumes:
            - 'sail-pgadmin:/var/lib/pgadmin'
        networks:
            - sail
        depends_on:
            - pgsql
    # rabbitmq:
    #     image: 'rabbitmq:4-management-alpine'
    #     hostname: rabbitmq
    #     ports:
    #         - '${FORWARD_RABBITMQ_PORT:-5672}:5672'
    #         - '${FORWARD_RABBITMQ_DASHBOARD_PORT:-15672}:15672'
    #         environment:
    #             RABBITMQ_HOST: '%'
    #             # Ensure a default user and password are present (can be overridden in .env)
    #             RABBITMQ_DEFAULT_USER: '${RABBITMQ_USER:-guest}'
    #             RABBITMQ_DEFAULT_PASS: '${RABBITMQ_PASSWORD:-guest}'
    #             RABBITMQ_VHOST: '${RABBITMQ_VHOST:-/}'
    #             RABBITMQ_DEFAULT_VHOST: '${RABBITMQ_VHOST:-/}'
    #             RABBITMQ_QUEUE: '${RABBITMQ_QUEUE:-default}'
    #             # Definitions are loaded via conf.d/20-definitions.conf
    #         networks:
    #             - sail
    #         healthcheck:
    #             test:
    #                 - CMD
    #                 - rabbitmq-diagnostics
    #                 - '-q'
    #                 - ping
    #             retries: 3
    #             timeout: 5s
networks:
    sail:
        driver: bridge
volumes:
    sail-pgsql:
        driver: local
    sail-pgadmin:
        driver: local
    # sail-rabbitmq:
    #     driver: local