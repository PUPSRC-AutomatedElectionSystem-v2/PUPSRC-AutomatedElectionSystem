vendor/bin/sail up -d --build --force-recreate && vendor/bin/sail composer update modules/organization-admin --no-scripts && vendor/bin/sail exec -u root laravel.test php artisan package:discover --ansi && vendor/bin/sail php artisan migrate:fresh && vendor/bin/sail exec rabbitmq rabbitmqctl add_user app app || true && vendor/bin/sail exec rabbitmq rabbitmqctl add_vhost / || true && vendor/bin/sail exec rabbitmq rabbitmqctl set_permissions -p / app ".*" ".*" ".*" && vendor/bin/sail exec rabbitmq rabbitmqctl list_permissions -p /